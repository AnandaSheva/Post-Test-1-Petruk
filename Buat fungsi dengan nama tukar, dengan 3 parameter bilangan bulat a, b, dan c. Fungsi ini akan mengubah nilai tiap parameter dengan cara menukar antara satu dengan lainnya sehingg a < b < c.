#include <iostream>
#include <cmath>

using namespace std;

void tukar(int &a, int &b, int &c){
    if(a>=b){
        swap(a,b);
    }
    if(b>=c){
        swap(b,c);
    }
    if(a>=b){
        swap(a,b);
    }
}
